# PostgreSQL (Docker command is in README)
DATABASE_URL="postgresql://ggcrafter:ggcrafter@localhost:5432/ggcrafter?schema=public"
PORT=3001
WEB_ORIGIN="http://localhost:5173"
# Token from @BotFather. Keep empty only for local demo mode.
TELEGRAM_BOT_TOKEN=""
# Test-only seed password. Change this before any public deployment.
ADMIN_PASSWORD="6725"
# Long random string used to sign short-lived admin sessions.
ADMIN_JWT_SECRET="replace-with-a-long-random-secret"
